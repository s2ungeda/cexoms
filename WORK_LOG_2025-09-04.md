# Work Log - 2025-09-04

## 작업 요약
대시보드 데이터 표시 문제 해결, 시스템 시작 자동화, Vault 영구 저장소 구현

## 완료된 작업

### 1. 대시보드 마켓 데이터 표시 문제 해결
- **문제**: 마켓 데이터와 잔고가 대시보드에 표시되지 않음
- **원인**: 
  - Orderbook 데이터 대신 24hr ticker 데이터가 필요했음
  - 백엔드와 프론트엔드 간 데이터 형식 불일치
- **해결**: 
  - `cmd/binance-market-full/main.go`: depth20 → ticker 스트림으로 변경
  - `dashboard/server/main_real.go`: 마켓 데이터 변환 로직 추가
  - ticker 데이터 구조체 및 파싱 함수 구현

### 2. Balance 서비스 Vault 통합
- `cmd/binance-spot-balance/main.go`: Vault에서 API 키를 가져오도록 수정
- 환경변수 fallback 지원 유지

### 3. 시스템 시작 자동화
- **개발용 스크립트**: 
  - `scripts/start-oms.sh`: 전체 시스템 자동 시작
  - `scripts/stop-oms.sh`: 전체 시스템 중지
- **Makefile 명령어 추가**:
  - `make start-all`: 전체 OMS 시작
  - `make stop-all`: 전체 OMS 중지
  - `make infra-up`: Redis 추가
- **Docker Compose 개선**: Redis 서비스 추가

### 4. Vault 영구 저장소 구현
- **문제**: 재시작 시마다 API 키가 사라짐 (개발 모드 문제)
- **해결**:
  - Makefile의 `run-vault`: 파일 기반 영구 저장소 사용
  - `configs/vault-config.hcl`: Vault 설정 파일
  - `scripts/init-vault.sh`: Vault 초기화 및 자동 unseal
  - 자동 unseal 로직을 `start-oms.sh`에 통합
  - 키 저장 위치: `~/.mExOms/vault-data/`

### 5. 보안 개선
- `.env` 파일의 보안 위험성 설명
- Vault 영구 저장소로 안전한 키 관리
- 시스템 키링 사용 옵션 제공

## 진행 중인 작업
없음

## 다음 작업 계획
- 추가 거래소 커넥터 구현 (Bybit, OKX, Upbit)
- 프로덕션 배포 준비
- 모니터링 및 알림 시스템 강화

## Phase 진행 현황
- 전체: 23/23 완료 (Phase 23까지 모두 완료)
- 추가 개선 작업 진행 중

## 주요 변경사항

### 코드 변경
1. **Market Service** (`cmd/binance-market-full/main.go`):
   - OrderBook 관련 코드 제거
   - 24hr Ticker 데이터 구조 추가
   - WebSocket URL: `@depth20@100ms` → `@ticker`

2. **Dashboard Server** (`dashboard/server/main_real.go`):
   - `handleMarketUpdate()`: 데이터 변환 로직 추가
   - 프론트엔드 형식으로 변환: `{price, change, change_pct, ...}`

3. **Balance Service** (`cmd/binance-spot-balance/main.go`):
   - Vault 클라이언트 import 추가
   - API 키 획득 우선순위: 환경변수 → Vault

### 인프라 변경
- Vault: 개발 모드 → 영구 저장소 모드
- Docker Compose: Redis 서비스 및 볼륨 추가
- 자동 시작/중지 스크립트 추가

## 참고사항
- **시작 명령**: `make start-all` 또는 `./scripts/start-oms.sh`
- **중지 명령**: `make stop-all` 또는 `./scripts/stop-oms.sh`
- **첫 실행 시**: Vault 초기화 자동 진행
- **API 키 설정**: `./scripts/store-binance-keys.sh` 실행
- **로그 위치**: `logs/` 디렉토리